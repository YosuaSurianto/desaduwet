/**
 * Cinematic film-grain layer sitting above the video, below the content.
 * Pure CSS (see `.film-grain` in styles/scroll.css) — no JS cost.
 */
export default function GrainOverlay() {
  return <div className="film-grain" />;
}
